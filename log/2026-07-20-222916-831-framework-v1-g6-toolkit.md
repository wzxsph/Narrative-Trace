# Framework V1 Phase 5 — G6 Toolkit

- Completed at: `2026-07-20T14:29:16.832Z`
- Scope: anonymous playtest records, recomputable G6 metrics, evidence application, attribution receipts, and tier rebuild behavior.
- PRD files changed: no.
- Online collection or external deployment: no.

## Implementation

- Added closed investigation playtest record and batch schemas for 5–8 consenting anonymous participants.
- Limited stored fields to anonymous IDs, consent, necessary local trace summaries, and boolean interview outcomes.
- Added a package-driven G6 evaluator that binds evidence to an exact content-pack input digest and recomputes all eight fixed metrics from per-player records.
- Added per-record and per-trace evidence digests plus the unified `narrative_gate_result_v1` output.
- Added stable CLI exit codes: 0 passed/applied, 1 valid gate failure, 2 invocation or configuration error.
- Added digest-bound playtest attribution for failed valid batches and deterministic tier rebuild logic: pass clears debt; failure records content/loop/inconclusive attribution and downgrades the loop and dependent packs to Experimental.
- Invalid, malformed, nonconsenting, stale, or build-mismatched data is never applicable as a tier decision.
- Kept the repository investigation package at `tier=verified` + `verification.status=debt`; no synthetic record was applied.

## Verification

- `python3 -m unittest tests.test_framework_v1_g6 -v`: 5 passed.
- `python3 -m unittest discover -s tests -v`: 107 passed in 26.290 seconds.
- `python3 scripts/validate_pack.py content_packs/missing_phone/v1 --through G5`: G1–G5 passed and the report was refreshed for the updated loop-package digest.
- Pass/debt-clear and fail/Experimental transitions were tested only against temporary loop and pack manifest copies.
- Current repository tier audit: loop `verified/debt`, pack `verified/debt`, `experimental_notice=false`.
- `git diff --check`: passed.
