# V0.25 基础键盘可访问性 Smoke 迭代

开始时间：2026-07-08 12:21:24.840 +0800  
当前记录时间：2026-07-08 12:28:41.607 +0800

## 本轮目标

继续执行产品迭代循环。V0.24 已把存档合同跑过真实 UI；V0.25 针对产品级缺口中的“键盘/无障碍”先补基础键盘路径：路径图开关和首个 observe 不应只依赖鼠标或触屏。

## 关键节点

- 12:21:24.840：归档 V0.24 PRD/技术文档到 `doc/prd/old version/2026-07-08-122124-840`。
- 12:22 左右：`index.html` 为路径按钮增加 `aria-controls` 和 `aria-expanded`。
- 12:23 左右：`src/app.js` 增加路径图开关、ARIA 同步、移动视口 inert 管理和 Escape 关闭。
- 12:23 左右：`src/styles.css` 增加 `button:focus-visible`。
- 12:24 左右：新增 `scripts/browser_a11y_smoke.py`。
- 12:24：运行 `python3 scripts/browser_a11y_smoke.py` 通过。
- 12:24-12:25：更新 README、PRD、技术文档、产品推衍记录和静态 hook。
- 12:26-12:28：完成完整结构、内容、保存契约、浏览器与全结局矩阵验证。

## 文件变化

新增：

- `scripts/browser_a11y_smoke.py`
- `log/2026-07-08-122537-675-v025-keyboard-a11y-smoke-loop.md`

修改：

- `index.html`
- `src/app.js`
- `src/styles.css`
- `tests/test_demo_contract.py`
- `README.md`
- `doc/prd/PRD_V0_文字冒险游戏框架.md`
- `doc/prd/PRD_V0_产品推衍记录.md`
- `doc/prd/agent_game_generation_technical_design_v0.md`

归档：

- `doc/prd/old version/2026-07-08-122124-840/PRD_V0_文字冒险游戏框架.md`
- `doc/prd/old version/2026-07-08-122124-840/PRD_V0_产品推衍记录.md`
- `doc/prd/old version/2026-07-08-122124-840/agent_game_generation_technical_design_v0.md`

## 已完成验证

局部验证已通过：

```bash
python3 scripts/browser_a11y_smoke.py
```

结果：

- 路径图可通过 Tab + Enter 打开。
- Escape 可关闭路径图并恢复焦点。
- 首个 observe 可通过 Tab + Enter 展开。

完整验证矩阵已通过：

```bash
python3 scripts/generate_game.py --brief examples/briefs/missing_phone.json --out generated/missing_phone_v0 --provider offline
python3 scripts/validate_json_schema.py generated/missing_phone_v0/game.json
python3 scripts/validate_game.py generated/missing_phone_v0/game.json
python3 scripts/content_qa_report.py generated/missing_phone_v0/game.json
python3 scripts/repair_game.py generated/missing_phone_v0/game.json
python3 scripts/smoke_playthrough.py generated/missing_phone_v0/game.json
python3 scripts/validate_model_output_archive.py
python3 scripts/validate_save_contract.py
python3 scripts/browser_smoke.py
python3 scripts/browser_save_contract.py
python3 scripts/browser_a11y_smoke.py
python3 scripts/browser_e2e_matrix.py
python3 -m py_compile gamegen/*.py scripts/*.py tests/*.py
node --check src/app.js
python3 -m unittest discover -s tests -v
git diff --check
```

结果：

- JSON Schema、结构校验、内容 QA、模型输出归档校验、保存契约均通过。
- 修复器确认当前生成物无需要修复的目标。
- 确定性 smoke playthrough 到达 `ending_publish`。
- 浏览器基础 smoke 通过，覆盖旧存档迁移、损坏存档恢复、不兼容存档提示。
- 浏览器保存契约回放通过，覆盖 v1 迁移、v2 结局恢复、未来版本 fallback、损坏 JSON fallback。
- 浏览器可访问性 smoke 通过，覆盖路径图键盘开关、Escape 关闭、observe 键盘触发。
- 浏览器全结局矩阵通过，覆盖 `ending_publish`、`ending_bury`、`ending_confront`。
- Python 编译、`node --check`、41 个单元测试和 `git diff --check` 均通过。

## 产品判断

V0.25 解决的是基础键盘路径，不是完整无障碍。它证明核心交互不是只靠鼠标/触屏幸运路径成立，但屏幕阅读器朗读质量、焦点陷阱完整性、多浏览器和真实设备仍未完成。

## 提交判断

V0.25 达到提交标准。下一轮应继续从“完整产品级距离”评估里选择一个仍短缺但可验证的缺口，而不是堆叠无 schema 约束的新玩法。
