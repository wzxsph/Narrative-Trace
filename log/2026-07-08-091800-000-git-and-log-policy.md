# Iteration Log: Git Init and Log Policy

Date: 2026-07-08

## Summary

- Initialized the project as a git repository.
- Added `.gitignore` rules for research and human-facing document folders.
- Added `.env` and Python cache ignores to avoid committing secrets or generated cache files.
- Created the `log/` folder as the project iteration log location.
- Updated `agent.md` so future iterations must record what changed.

## Files Changed

- `.gitignore`
- `agent.md`
- `log/2026-07-08-git-and-log-policy.md`

## Notes

- `reference/` remains local research material and is ignored by git.
- `给人类看的文档/` and `doc/给人类看的文档/` are ignored to avoid token-heavy human-facing document churn.
- Future work should add one concise Markdown entry under `log/` for each meaningful iteration.

