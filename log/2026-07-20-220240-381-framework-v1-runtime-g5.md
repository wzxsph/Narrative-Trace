# FRAMEWORK V1 包驱动运行时与 G5 迭代

- 开始时间：2026-07-20 20:06:00.000 +0800
- 结束时间：2026-07-20 22:02:40.382 +0800

## 本轮完成

- 将单体浏览器脚本拆为 bootstrap、内容包 loader、通用状态/行动引擎、V1 存档、text/fallback surface renderer 与 investigation UI adapter。
- 根页面改由 `runtime-config.json` 选择内容包，作品路径、初值、状态标签、画像回声、章节元数据和存档命名不再硬编码在玩家端。
- 新存档使用 `narrative_trace.save.<pack_id>.<pack_version>`；首次加载兼容迁移旧键中的 v1/v2 payload，先写新键、成功后才清除旧键，迁移失败提供可见 fallback。
- 保持原竖屏 UI、单链 observe 展开、行动栏、章节复盘、路径图、三结局、重载恢复和键盘操作。结局页新增 `protected / harmed / believed` 数据化画像。
- image/html 在 V1 不执行专用内容，只展示必填文本 fallback，并继续暴露可访问的 anchor 行动；Experimental 包在内容开始前强制显示确认页。
- 新增 G5：重放 G4 自动 witness、存档迁移与包隔离、移动/桌面视口、键盘路径、image/html fallback 和 Experimental 阻断提示。
- 静态与 Cloudflare Worker 构建均强制 `--pack`，且只收录被选内容包、运行时和引用文件；本轮未部署外部环境。
- 更新 README、四层架构图和 G1–G6 门禁图；未修改 PRD。

## 主要文件

- 新增：`runtime-config.json`、`src/runtime/{bootstrap,pack-loader,engine,save,text-surface-renderer,investigation-ui}.js`
- 修改：`src/app.js`（缩减为 bootstrap 入口）、`src/styles.css`
- 新增：`gamegen/g5.py`、`scripts/build_static_bundle.py`
- 修改：`scripts/validate_pack.py`、浏览器存档测试、Worker 构建脚本与 Pages workflow
- 新增：`tests/runtime_contract.mjs`、`tests/test_framework_v1_runtime.py`
- 新增：`content_packs/missing_phone/v1/verification/g1-g5.json`
- 更新：`README.md`、`doc/readme_diagrams/readme_diagrams.html` 及两张渲染图

## 验证

- `python3 scripts/validate_pack.py content_packs/missing_phone/v1 --through G5`：G1–G5 全绿。
- G5：3 个结局 witness 全部由浏览器重放；旧存档 write-then-clear、坏档 fallback、包间隔离、键盘、390×844/1280×900、image/html fallback、Experimental 确认均通过。
- `python3 scripts/browser_e2e_matrix.py`：三结局、两次章节复盘、结局重载和重开全部通过。
- `browser_smoke.py`、`browser_save_contract.py`、`browser_a11y_smoke.py`、`browser_omission_paths.py`：全部通过。
- `python3 -m unittest discover -s tests -v`：97 项通过（原 81 项基线保持通过）。
- 静态 bundle 与 Worker bundle 均以 `--pack content_packs/missing_phone/v1` 构建成功；未执行部署。

## 遗留与下一步

- Agent 生成管线仍需改为包/玩法驱动并接入四类 DecisionReceipt。
- G6 真人样本尚未提供；调查玩法包继续保持 `Verified (debt)`。
